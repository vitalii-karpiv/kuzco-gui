/** Re-export from the shared location — import from `@/shared/date-range` directly for new code. */
export {
  type FinanceDateRange,
  defaultFinanceRange,
  rangeToIso,
  rangeToSeconds,
} from "@/shared/date-range";
